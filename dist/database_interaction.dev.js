"use strict";

var mongoose = require('mongoose');

var story = require("./models/stories");

var readline = require('readline');

function main() {
  rl.question("Enter model to alter 1) Stories, ..", function (model) {
    rl.question("What do you want to do?");
  });
}